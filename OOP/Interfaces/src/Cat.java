public class Cat implements Pet { // because we implemented the Pet interface, we MUST define all the methods that are inside the interface

    public void play() {
        System.out.println("The cat plays with its owner");
    }

}
