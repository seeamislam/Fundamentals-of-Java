public interface Pet { // created a Pet interface
    void play(); // initialize the signature of the method, but nothing else.
    // this means that any other classes that implement this interface, MUST have a play method inside it.

}
